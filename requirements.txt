fastapi>=0.110
uvicorn>=0.29
httpx>=0.27
pytest>=8.0
python-dotenv>=1.0

# LLM SDK — Anthropic is here as a convenience. You may swap in openai, or any
# other provider's SDK; the task does not require a specific model vendor.
anthropic>=0.40
